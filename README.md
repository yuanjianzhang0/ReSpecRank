# ReSpecRank

ReSpecRank is a PyTorch repository for cross-sectional stock ranking using
regime-adaptive joint temporal and graph spectral filters. It includes local-data
preprocessing, model training, evaluation, diagnostics, configurations, and tests.
